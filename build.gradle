// build.gradle (raiz)
// Arquivo mínimo e correto para projetos Android modernos

buildscript {
    repositories {
        google()
        mavenCentral()
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
